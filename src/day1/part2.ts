const part2 = (expenseReport: Array<number>): number => 0;

const whichThreeNumbersAddUpTo = (
  list: Array<number>,
  target: number
): Array<number> => [0, 0, 0];

export default part2;
export { whichThreeNumbersAddUpTo };
