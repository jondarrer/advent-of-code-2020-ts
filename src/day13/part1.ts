const part1 = (input: Array<number | Array<number | string>>): number => 0;

const arrivesAtOrAfter = (period: number, target: number): number => 0;

export default part1;
export { arrivesAtOrAfter };
