const part1 = (input: Array<number>, range = 25): number => 0;

const whichNumbersSumToTarget = (nums: Array<number>, target: number): number =>
  0;

export default part1;
export { whichNumbersSumToTarget };
